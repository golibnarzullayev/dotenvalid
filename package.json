{
  "name": "dotenvalid",
  "version": "1.0.3",
  "description": "A minimal TypeScript-based package for loading and validating .env files with type checking",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsup src/index.ts --format esm,cjs --minify --dts",
    "test": "echo \"No tests specified\" && exit 0",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "dotenv",
    "environment",
    "env",
    "type",
    "validation",
    "typescript"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/golibnarzullayev/dotenvalid.git"
  }
}
